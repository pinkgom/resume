import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  base: process.env.NODE_ENV === 'production' ? '/resume/' : '/',
  define: {
    __IMAGES_BASE_PATH__: process.env.NODE_ENV === 'production' ? '"/resume"' : '""'
  },
  server: {
    port: 3000,
    open: true
  },
  build: {
    outDir: 'dist',
    assetsDir: 'assets'
  }
})